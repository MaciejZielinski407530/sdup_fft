

OPTION psf_version = 2.1;

BEGIN DRIVER fft_parallel_ip
	OPTION supported_peripherals = (fft_parallel_ip);
	OPTION copyfiles = all;
	OPTION VERSION = 1.0;
	OPTION NAME = fft_parallel_ip;
END DRIVER
